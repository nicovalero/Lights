<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1050">
    <Canvas>
        <ListView Height="580" Width="500" ItemsSource="{DynamicResource MidiEffectLinks}" HorizontalAlignment="Center" VerticalAlignment="Top">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Channel" DisplayMemberBinding="{Binding ChannelName}"/>
                    <GridViewColumn Header="Note" DisplayMemberBinding="{Binding NoteName}"/>
                    <GridViewColumn Header="Velocity" DisplayMemberBinding="{Binding Velocity}"/>
                    <GridViewColumn Header="Effect" DisplayMemberBinding="{Binding EffectName}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Label Canvas.Left="573" Canvas.Top="50" Width="120" Content="Channel" HorizontalContentAlignment="Center"/>
        <ListBox Canvas.Left="573" Canvas.Top="78" Width="120" HorizontalAlignment="Center" Height="90" VerticalAlignment="Top" ItemsSource="{DynamicResource AvailableMidiChannelList}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Height="30">
                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Label Canvas.Left="719" Canvas.Top="50" Width="120" Content="Note" HorizontalContentAlignment="Center"/>
        <ListBox Canvas.Left="719" Canvas.Top="78" Width="120" ItemsSource="{DynamicResource AvailableMidiNoteList}" HorizontalAlignment="Center" Height="90" VerticalAlignment="Top">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Height="30">
                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Label Canvas.Left="865" Canvas.Top="50" Width="120" Content="Velocity" HorizontalContentAlignment="Center"/>

        <ListBox Canvas.Left="865" Canvas.Top="78" Width="120" ItemsSource="{DynamicResource AvailableMidiVelocityList}" HorizontalAlignment="Center" Height="90" VerticalAlignment="Top">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Height="30">
                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Label Canvas.Left="573" Canvas.Top="204" Width="120" Content="Effect" HorizontalContentAlignment="Center"/>
        <ListBox Canvas.Left="573" Canvas.Top="234" Width="120" ItemsSource="{DynamicResource HueEffectList}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Height="30">
                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Label Canvas.Left="719" Canvas.Top="204" Width="120" Content="Color" HorizontalContentAlignment="Center"/>
        <ComboBox Canvas.Left="719" Canvas.Top="234" Width="120"/>

        <Button Content="Link" Canvas.Left="719" Canvas.Top="306" Width="120" Height="70" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <Button Content="Connect Bridges" Canvas.Left="662" Canvas.Top="396" Height="85" Width="234" HorizontalAlignment="Left" VerticalAlignment="Top" Click="ConnectBridgesButton_Click"/>
    </Canvas>
</Window>
